# Modules are small libraries - math example. 

from math import pi
import sys
import random as rdm
from enum import Enum
import person
from rps7 import rock_paper_scissors

print(person.favourite_car)
person.randomFunFact()

print(person.__name__)


rock_paper_scissors()